/* global window */
// Runtime configuration — overwritten by the container entrypoint at start.
// Placeholder values are replaced with real env vars before the SPA loads.
window.__SOVECOM__ = {
  apiBaseUrl: '__API_BASE_URL__',
};
