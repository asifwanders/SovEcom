{
  "name": "@sovecom/module-notify-back-in-stock",
  "version": "0.1.0",
  "private": true,
  "description": "Notify-back-in-stock — a SovEcom reference module (guest-friendly restock email notifications).",
  "license": "AGPL-3.0-only",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src test --ext .ts,.tsx",
    "test": "vitest run",
    "test:watch": "vitest",
    "contract": "module-contract-tests .",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@sovecom/module-sdk": "workspace:^"
  },
  "devDependencies": {
    "@sovecom/module-contract-tests": "workspace:*",
    "@sovecom/theme-sdk": "workspace:*",
    "@types/node": "^24.0.0",
    "@types/react": "^19.2.17",
    "react": "^19.2.7",
    "typescript": "^5.4.0",
    "vitest": "^4.0.0"
  }
}
