{
  "name": "@sovecom/client-js",
  "version": "0.1.0",
  "private": true,
  "license": "AGPL-3.0-only",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "generate": "openapi-typescript ../../apps/api/openapi.json -o src/generated/api.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "openapi-typescript": "^7.4.0",
    "typescript": "^5.4.0",
    "vitest": "^4.0.0"
  }
}
